# Configuración local de pnpm para Backend
# Auto-instalar peers (dependencias peer automáticas)
auto-install-peers=true

# Permitir builds ignorados
ignore-build-errors=true

# Permitir scripts de build para estos paquetes
onlyBuiltDependencies=pnpm,sqlite3
