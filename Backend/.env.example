# Ejemplo de variables de entorno para Backend
PORT=3000

# Base de datos - PostgreSQL
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=elemental
DB_USER=postgres
DB_PASS=postgres_password

# Seguridad
JWT_SECRET=tu_secreto_super_secreto_y_largo

# SQLite (opcional, para desarrollo local)
# DB_USE_SQLITE=true
# DB_SQLITE_PATH=./elemental_battlecards.sqlite

# HTTPS (opcional, solo si tienes certificados SSL)
# CERT_FILE=./cert.pem
# KEY_FILE=./key.pem
